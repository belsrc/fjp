/**
 * Negates the given boolean-like value.
 * @signature not :: Boolean -> Boolean
 * @func
 * @example
 * not(true); // false
 * not(false); // true
 * not(2); // false
 */
const not = x => !x;

export default not;
