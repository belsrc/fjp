import 'babel-core/register';
import test from 'ava';
import average from './index';

test.todo('these tests need to be checked');

// test('returns zero for null arg', t => {
//   const expected = 0;
//   const actual = average();
//
//   t.is(actual, expected);
// });
//
// test('returns number for array', t => {
//   const arr = [ 1, 2, 3 ];
//   const expected = 'number';
//   const actual = average(arr);
//
//   t.is(typeof actual, expected);
// });
//
// test('returns number for arg list', t => {
//   const expected = 'number';
//   const actual = average(1, 2, 3);
//
//   t.is(typeof actual, expected);
// });
//
// test('returns correct average for array', t => {
//   const arr = [ 1, 2, 3 ];
//   const expected = 2;
//   const actual = average(arr);
//
//   t.is(actual, expected);
// });
//
// test('returns correct average for arg list', t => {
//   const expected = 2;
//   const actual = average(1, 2, 3);
//
//   t.is(actual, expected);
// });
