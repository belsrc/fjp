import 'babel-core/register';
import test from 'ava';
import concatN from './index';

test.todo('these tests need to be checked');

// test('returns empty string for no args', t => {
//   const expected = '';
//   const actual = concat();
//
//   t.is(actual, expected);
// });
//
// test('returns empty array for empty array', t => {
//   const actual = concat([]);
//
//   t.true(Array.isArray(actual) && actual.length === 0);
// });
//
// test('returns correctly concatenated strings', t => {
//   const expected = 'hello world foo bar';
//   const actual = concat('hello', ' ', 'world', ' foo', ' bar');
//
//   t.is(actual, expected);
// });
//
// test('returns correctly concatenated arrays', t => {
//   const expected = [
//     'foo',
//     'bar',
//     'hello',
//     'world',
//   ];
//   const actual = concat([ 'foo', 'bar' ], [ 'hello', 'world' ]);
//
//   t.deepEqual(actual, expected);
// });
