/**
 * Get the first value of the given array.
 * @arg {Array} arr
 * @signature head :: [a] -> a
 * @func
 * @example
 * head([1, 2, 3, 4, 5]) // [1]
 */
const head = ([x]) => x;

export default head;
