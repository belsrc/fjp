/**
 * Get the first value of the given array.
 * @func
 * @signature head :: [a] -> a
 * @arg {Array} arr
 * @example
 * head([1, 2, 3, 4, 5])
 * // [1]
 */
const head = ([x]) => x;

export default head;
